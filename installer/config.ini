[Banco]
; Parametros de conexao com o PostgreSQL (editaveis sem recompilar o app).
Host=localhost
Porta=5432
Usuario=postgres
Senha=postgres
Database=funcionarios
; LibPq vazio = o app procura libpq.dll na propria pasta do .exe.
; Preencha apenas se o cliente 32-bit estiver em outro caminho.
LibPq=
